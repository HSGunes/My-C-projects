#include <stdio.h>
#include <stdlib.h>


#define SIZE 6
int merge(int array[], size_t left) {

    int left_Arr[SIZE];
    int right_Arr[SIZE];
        for (size_t i = 0; i <left; i++) {
            left_Arr[i] = array[i];
        }
        for (size_t i = left; i < SIZE; i++) {
            right_Arr[i] = array[i];
        }
}
int 
int main() {
    int arr[SIZE] = { 12, 11, 13, 5, 6, 7 };
 

    return 0;
}
